import React from 'react'; function EventForm({ formData, handleChange, handleSubmit, previewImage, buttonLabel }) { return (<form onSubmit={handleSubmit}><input type="text" name="title" placeholder="Title" value={formData.title} onChange={handleChange} required /><input type="datetime-local" name="date" value={formData.date} onChange={handleChange} required /><input type="text" name="location" placeholder="Location" value={formData.location} onChange={handleChange} required /><textarea name="description" placeholder="Description" value={formData.description} onChange={handleChange} rows="4" required></textarea><input type="file" name="image" onChange={handleChange} /><input type="text" name="price" placeholder="Entry Fee" value={formData.price} onChange={handleChange} required /><button type="submit" className="btn btn-primary w-100">{buttonLabel}</button></form>); } export default EventForm;